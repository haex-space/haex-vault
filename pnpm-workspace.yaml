# pnpm 10 no longer reads `overrides` / `onlyBuiltDependencies` from the
# `pnpm` field in package.json — they live here now. Rationale for each
# override is documented in docs/dependency-overrides.md. Do not add new pins
# here without updating that file.
overrides:
  vue-router: "^5.0.6"
  zod: "^3.25.76"
  defu: ">=6.1.5"
  picomatch: ">=4.0.4"
  node-forge: ">=1.4.0"
  lodash: ">=4.18.0"
  axios: ">=1.15.0"
  happy-dom: ">=20.8.9"
  h3: ">=1.15.6"
  "@xmldom/xmldom": ">=0.8.13"
  esbuild: ">=0.28.1"
  js-yaml: "^4.2.0"
  undici: "^7.28.0"

onlyBuiltDependencies: []
